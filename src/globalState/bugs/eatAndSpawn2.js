//import getNewOffspring2 from "./getNewOffspring2";
import { Node, changeData } from './tree'

export default function eatAndSpawn2(
    bugId,
    bugs2
  ) {
    const newBugs2 = Object.assign(new Node(), bugs2)
  }

